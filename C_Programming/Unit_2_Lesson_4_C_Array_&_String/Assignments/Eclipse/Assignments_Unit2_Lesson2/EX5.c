/*
 * EX5.c
 * Solve Homework 3
 * Created on: Jan 5, 2024
 * Author: mahmoud Saad
 */
/*
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int n;
	int num,i,loc=1;
	printf("##########Console-output##########\n");
	printf("Enter no of elements : ");
	fflush(stdin);        fflush(stdout);
	scanf("%d",&n);
	int arr[n];
	for(i=0;i<n;i++)
	{
		fflush(stdin);        fflush(stdout);
		scanf("%d",&arr[i]);
	}
	printf("Enter the elements to be searched : ");

	fflush(stdin);        fflush(stdout);
			scanf("%d",&num);

			for(i=0;i<n;i++)
			{
				if(num == arr[i])
					break;
				loc++;
			}
			if(loc == 1)
               printf("This Number is not exist in the array");

			if(loc> 1)
				printf("Number found at the location = %d",loc);



}
*/
